///ae2b spoj problem
#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int gcd(int a, int b) {
    return b == 0 ? a : gcd(b, a % b);
}

int main()
{
	 int T;
	 int K,N,x1,y1,x2,y2;

	 int x_tocover = abs(x2-x1);
	 int y_tocover = abs(y2-y1);

	 
return 0;
} 