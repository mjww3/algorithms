#include <iostream>
#include <stdio.h>
#include <math.h>
#include <string>
#include <algorithm>
#include <vector>
using namespace std;
int distance(vector<int>a,vector<int>b,int d)
{
	int dist = 0;
	int sum  =0;
	for(int i=0;i<2;i++)
	{
		sum = sum+abs((a[i]-b[i]));
	}
	dist = (sum);
}
int main()
{
	int n;
	cin>>n;
	while(n--)
	{
		

	}
}